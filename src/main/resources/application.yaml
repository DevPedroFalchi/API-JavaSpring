spring:
  application:
    name: ex-semana3
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
  #      nao sao necessarias jpa
  jpa:
    hibernate:
      # se nao existe definicao desse atributo, ele é none por default
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
